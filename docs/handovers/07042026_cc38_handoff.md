# Session CC38 Handover
**Date:** 2026-07-04
**Agent:** Archer (CTO)
**Focus:** Beacon upstream/downstream analysis → full conversion/retention build-out. Seven PRs (6 features + 1 critical hotfix) across clearpath-beacon + waypoint, all merged. One manual step outstanding (Cloudflare `CRON_SECRET`).

> Triggered by: "run start process" → "analyze beacon upstream and downstream, suggest improvements that will make this the go-to platform for counselors" → "Do everything but stripe."

---

## 1. The analysis (three parallel exploration agents + synthesis)

Mapped Beacon's full pipeline: upstream funnel (marketing → trial → onboarding → Zelle purchase), product surface (17 deep pages, local-first IndexedDB v6, cloud hard-disabled, **zero AI** — all drafting is templates), downstream outputs (3-layer PDF attestation moat, Morning Brief retention, CREST auto-derive).

**Verdict:** the product core is narrow-but-deep and defensible; the leaks were in the pipe — no trial nurture, no renewal warning, no purchase path in-app, single-device lock-in, and a school-year boundary (= the renewal moment) with no year-over-year story.

**Subagent false-positives corrected during synthesis** (3rd/4th instances of the CC30/CC32 "false-missing" pattern): "trial expiry not enforced" (it IS — soft gate in db.js, the model CC37 copied), "no CSV export" (existed on Students + Time Tracker), "no trial countdown" (existed at ≤4 days). Always verify subagent audit claims against the actual code path before building.

**Memory corrected:** Beacon is **$79/yr / $8/mo** (consistent everywhere), not the $999 in the stale memory file.

## 2. CRITICAL production hotfix (found + fixed + merged mid-build)

**[clearpath-beacon#10](https://github.com/jkculley-cyber/clearpath-beacon/pull/10) (`0b5fde4`)** — `/setup` **crashed** ("Beacon crashed — useEffect is not defined") for every returning user (`?signin=1`) and every post-purchase activation (`activate.html` → `/setup?key=BCN-…`) since `e7da177` (Apr 28): `ForgotLicenseLink` used `useEffect` without importing it, and `showLicenseField` initializes TRUE on exactly those paths. Fresh-trial visitors never saw it (field starts collapsed) — which is why it survived 9+ weeks. One-line fix, browser-verified, squash-merged ahead of everything else. **Lesson: an error boundary + a collapsed-by-default entry path can hide a total-crash bug from every smoke test that only walks the happy path. Test `?signin=1` and `?key=` variants explicitly after touching setup-page components.**

## 3. What shipped (all merged to main, hosted app auto-deploys)

| PR | What | Key mechanics |
|---|---|---|
| [#8](https://github.com/jkculley-cyber/clearpath-beacon/pull/8) conversion | License renewal banner (30d: info/amber/red, sticky ≤7d) — nothing watched `expires_at` before; Morning Brief trial deadline from day 7 + renewal deadline from day 30; Settings upgrade card (price, Zelle steps, key-by-email, data-stays) + expiry date + renewal note | `getLicenseDaysLeft()` in license.js; `licenseDaysLeft` in AuthContext |
| [#9](https://github.com/jkculley-cyber/clearpath-beacon/pull/9) export | CSV export on Sessions (current filter) + Communications (contact log incl. tracking #); "Open in Email" hands filled templates to the counselor's own mail client via `mailto:` (nothing sent from Beacon; PII-free subject) | shared `csvExport.js`, quoted cells, CRLF, UTF-8 BOM (verified byte-level — note: `Blob.text()` strips BOMs, use `arrayBuffer` to verify) |
| [#11](https://github.com/jkculley-cyber/clearpath-beacon/pull/11) multi-device | "Restore from backup folder (OneDrive/Drive)" on `?signin=1`: picks the sync folder, restores the newest `beacon-backup-*` (scan logic unit-tested via mock handle), **persists the handle so the new device keeps backing up to the same folder** — both devices share one backup stream. Chromium-only, file-restore fallback intact | `findNewestBackupInFolder()` in backupFolder.js; decryption inputs validated before the picker |
| [#12](https://github.com/jkculley-cyber/clearpath-beacon/pull/12) EOY | School Year Transition now snapshots the year (license-exempt `settings` key `year_summary_<label>`) AND writes an archive backup **before any mutation**; Reports gets a Year-over-Year table (hidden until a prior year exists); Counselor Handoff Package PDF (tier-sorted caseload, open items, successor restore instructions, FERPA footer, deliberately un-hashed) | `yearSummary.js`, `handoffExport.js`; verified end-to-end incl. real transition with pre-mutation archive capture |
| [#13](https://github.com/jkculley-cyber/clearpath-beacon/pull/13) history | IndexedDB **v6→7**: `record_history` store (in backups); db.js hooks on students+sessions — updates log field diffs (no-op writes skipped), deletes keep snapshots; best-effort, **license-gate-exempt** (CC34 principle); History tab on StudentDetailPage, "detection, not prevention" framing | Dev gotcha hit: HMR mid-edit upgraded the dev DB to v7 *without* the new store (version bump edit ran before store-creation edit). Prod is safe (code ships atomically). If a dev DB is stuck: delete `beacon_local` |
| [waypoint#20→#21](https://github.com/jkculley-cyber/waypoint-intelligent-design-daep/pull/21) drip | `/api/beacon-drip` Pages Function: trial drip (welcome/day3/day10/day13, stops on license) + renewal emails (30d/7d, 7 suppresses late 30) via **existing** cpeg-site env (`OPS_SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`); idempotency state in ops `command_center` key `beacon_drip_log` (**no DDL**); catch-up sends only the latest due stage; 50-send cap; optout honored; GH Actions daily cron (14:00 UTC) + manual dispatch with dry-run; `computePlan` exported pure + unit-tested offline (incl. renewal_7-suppresses-renewal_30 bug caught by the test) | **NOT LIVE until the one manual step below** |

## 4. ⚠️ OUTSTANDING — one manual step (Kim)

The drip endpoint answers **503 by design** until `CRON_SECRET` is set in **Cloudflare Pages → cpeg-site → Settings → Variables and Secrets**. The matching GitHub secret `DRIP_CRON_SECRET` is **already set** on the waypoint repo (value shown in the session transcript at the point where it was generated). After pasting:
1. Actions → "Beacon Email Drip" → Run workflow (**dry run = true**) → inspect the JSON plan.
2. If the plan looks right, run again with dry run = false (or wait for the daily cron).
Until then the daily cron fails loudly — that's the reminder, not a bug.

## 5. Deliberately deferred
- **Stripe** — user's explicit call ("everything but stripe"). Zelle-only remains; ~$2.59/sale fee was the only recurring cost in the whole plan.
- **In-app email-code verification at setup** — welcome email doubles as a bounce detector without adding onboarding friction. Revisit only if bounce data shows a problem.
- SB-179/parent-contact `report_annotations` (CC35 leftover), district mode (demand-pull per DECISIONS), AI drafting (conflicts with the privacy pitch until asked for).

## 6. Verification notes
Every PR browser-verified on a real dev server (`beacon-dev` entry added to untracked `.claude/launch.json`, port 5183) with lead-capture endpoints stubbed so no test data hit prod `demo_leads`. All Cloudflare Pages checks green; "Workers Builds" red = known cosmetic stray connector (CC31), merged over per precedent.

## 7. State / risks
- **Active risks: X-2 only** (infra/uptime monitoring) — unchanged.
- cpeg-site dual-deploy-path race (CC32) still lurking, deploys fine this session.
- The 5 Beacon merges auto-deployed the hosted app; spot-check `beacon.clearpathedgroup.com/setup?signin=1` renders (crash fix live) when convenient.

## Post-closing addendum (same session, user follow-up)

**CountSel trademark removed from all customer-facing copy** at user direction ("I don't want countsel coming after us"): app landing page ("Switching from another time-tracking tool?"), DistrictPreview migration bullet, homepage Beacon card ("Replaces your standalone time tracker"), beacon-product.html district bullet. [beacon#14](https://github.com/jkculley-cyber/clearpath-beacon/pull/14) + [waypoint#22](https://github.com/jkculley-cyber/waypoint-intelligent-design-daep/pull/22), both merged; **verified live post-deploy: 0 countsel matches** on homepage, beacon-product.html, and the hosted app bundle (new copy confirmed present). Base64 payloads in downloads/ decoded and checked (CC37 lesson). Internal competitive-analysis docs deliberately untouched.

**DPA/backup question answered + a real guardrail gap closed:** cloud sync (our Supabase) would need a DPA and is already hard-disabled for everyone; **automatic backups stay for individuals** — they're encrypted local file writes, Clear Path never receives the data, so no DPA obligation on us. The counselor's folder choice is the only exposure, and the Settings picker's district-managed acknowledgment modal gates it. **Gap found in this session's own #11:** the new-device folder-restore persisted the folder handle WITHOUT that acknowledgment — fixed in beacon#14 (restore always proceeds; future auto-backups wire up only after the district-managed confirmation, decline still restores).

## What's next
1. **Kim: paste `CRON_SECRET` into Cloudflare** (section 4) → dry-run → live.
2. Manual click-through of the folder-restore flow on a real second browser profile (needs a human gesture the headless preview can't provide).
3. Standing: X-2 uptime monitoring; optional cpeg-site single-deploy-path cleanup.
4. Marketing follow-through from the analysis: link Facebook posts straight to `beacon-product.html`, lead with "the only counseling platform whose documents survive a due-process hearing."
