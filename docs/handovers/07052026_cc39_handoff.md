# Session CC39 Handover
**Date:** 2026-07-05
**Agent:** Archer (CTO)
**Focus:** Full Navigator review (correctness + compliance + funnel) → 12 fixes shipped in one PR + one applied migration + one applied ops-RLS lockdown.

> Triggered by: "run start process navigator focus" → "review navigator for any corrections or enhancements needed" → "yes" (do the fixes) → apply migration 086 → apply demo_leads RLS → "Commit +PR and run closing process".

---

## 1. The review (three parallel agents + independent verification)

Ran three background review agents over the Navigator surface — code correctness, compliance/data-integrity (migrations 037–086 vs frontend), and sales funnel/demo — then **verified every critical claim myself against real code, live SQL behavior, and the live site before accepting it** (the CC30/CC38 false-missing discipline). Unusually, the verified-critical findings held up with ~zero false positives; the one false positive caught was the agent's claim that `DashboardPage.jsx:544` shared the isAdmin bug (it uses a real local boolean).

## 2. What shipped — PR `fix/navigator-cc39-review` (commit `b047a44`)

**Correctness (frontend, build clean, 0 net-new lint errors):**
- **Campus scoping restored (highest-leverage).** `isAdmin` is exposed by AuthContext as a *function*; ~20 Navigator consumers used it as a bare boolean, so `!isAdmin` was always false → the `.in('campus_id', …)` filter never ran and campus-scoped staff saw **district-wide** student data (no RLS backstop — Navigator RLS is district-only). Fixed to `isAdmin()` in useNavigator, useDisproportionality, NavigatorStudentsListPage, NavigatorReportsPage.
- **Student roster import un-broken.** `importValidators.js` wrote the dropped `race` column (migration 070 renamed it `race_ethnicity`), failing the whole batch. Now writes `race_ethnicity` through a JS port of 070's normalizer (PEIMS codes, natural-language, blank→`not_specified`).
- **"Returning from DAEP" panel un-broken.** Added `transition_plans!transition_plans_incident_id_fkey` hint to the ambiguous embeds in useNavigator + useDaepDashboard (both silently returned `[]` via PGRST201). Also fixed the dead `students.race` column in the DAEP demographics query.
- **Escalation bulk-assign.** Placement-only students had `campus: null` (query didn't join). Hydrated them in the query + added a guard so an unresolved campus can't atomically fail the batch insert (`campus_id` is NOT NULL).
- **ISS kiosk.** Read the nonexistent `profile.campus_ids`; now uses `campusIds` from context.
- **School-year hardcodes (would break Aug 1, 2026).** `NavigatorStudentPage` (YOY tile + progress score) and `NavigatorPilotPage` (year `<select>`) now derive from `currentSchoolYear()`.
- **Date off-by-one.** Six date-only display sites switched from `new Date()` to `parseISO`; `aggregateByMonth` now parses year/month from the string (was binning a month late and dropping July in US timezones).
- **Hearing PDF.** Replaced the un-encodable `⚠` glyph (the legally load-bearing 10-day line) with `WARNING:` + sanitizer backstop; the `§300.530(g)` basis now prints on placements (a lawful removal no longer reads as a violation); "immutable" → "tamper-evident audit log" in the reason-history modal.

**Compliance — migration 086 (`086_navigator_sped_gate_hardening.sql`, APPLIED + VERIFIED live).**
`fn_navigator_check_sped_10day` rewritten to close four holes; trigger widened to `UPDATE OF days, start_date, end_date, placement_type, manifestation_determination_id, special_circumstances_basis`:
- **H-1:** a manifestation-positive MDR now requires a `special_circumstances_basis` (IDEA §300.530(g)); previously any linked MDR satisfied the gate.
- **H-2:** effective removal days = `COALESCE(days, end_date−start_date+1, 0)` in both the running sum and NEW — a blank Days field no longer hides a dated removal.
- **M-5:** the linked MDR must have `student_id = NEW.student_id`.
- **L-1:** start/end-date edits now re-fire the check.
This is the DB enforcement backbone: it **backstops the Edit-drawer and CSV-import bypass paths automatically** (both previously wrote `special_circumstances_basis`/MDR-checks nowhere) — an unlawful write now RAISEs regardless of entry point, and `err.message` surfaces the §300.530(e) explanation. UI parity: the New Placement drawer's `proposedDays` now derives from dates too so the warning still shows.
**Verified live** via SQL editor: `pg_get_functiondef … LIKE '%300.530(e)%'` = true; trigger def contains `start_date` = true.

**Funnel/marketing (verified live in preview):**
- Homepage Navigator card price: **"From $499/yr per campus · $2,500/yr district-wide"** (was "Starting at $2,500/yr" — 5× the wedge price; a campus AP self-disqualified at first touch).
- navigator.html: `$999/yr` renewal now disclosed; false **"Urgency levels, concern categories"** referral claim rewritten to "Skill-gap tagging, outcome triage, and campus routing" (matches what ships).
- Demo script + DECISIONS.md: corrected to the live cred `explore-navigator@clearpathedgroup.com / Explore2026!` and district UUID `33333333-…` (the old `ap@lincoln-hs.demo`/`22222222-…` were stale and the seed *deletes* that user every run — DECISIONS.md "wins over everything," so this would have re-propagated).

**Security — demo_leads PII leak (CONFIRMED live, then CLOSED).**
The public anon key embedded in navigator.html/waypoint.html allowed anonymous **SELECT** on the ops `demo_leads` table — an anonymous GET returned all 31 real prospect rows (name, school email, phone, district). Kim applied `docs/ops/demo_leads_rls_lockdown.sql` in the **ops** project (`xbpuqaqpcbixxodblaes`). **Verified after apply:** anon SELECT → `[]` (Content-Range `*/0`); anon INSERT → `201` (form still works). No lead loss — every submission also POSTs to Formspree (emails Kim).

## 3. Open items / follow-ups
- **DELETE the test row I inserted to verify INSERT:** `demo_leads` row `name = "CC39 RLS verify - delete me"` / `email = cc39-rls-verify@clearpathedgroup.com` — must be removed via the **ops SQL editor** (the /waypoint-admin panel can no longer read demo_leads with the anon key, so it won't show there).
- **/waypoint-admin demo-leads panel — RESTORE BUILT (pending deploy).** The lockdown broke the panel's anon SELECT/UPDATE/DELETE on ops demo_leads. Added `supabase/functions/ops-leads/index.ts` (main project) — verifies the caller is `waypoint_admin`, then uses the ops **service role** to list/update/delete demo_leads; rewired all `WaypointAdminPage` demo_leads calls (LeadsManager + DemoLeadsSection) through a new `opsLeads()` helper in `opsSupabase.js`. Mirrors the `invite-user` auth pattern; build clean. **Kim must, on the MAIN project (`kvxecksvkimcgwhxxyhw`):** (1) deploy the function — `supabase functions deploy ops-leads` (or dashboard upload); (2) set two secrets — `OPS_SUPABASE_URL = https://xbpuqaqpcbixxodblaes.supabase.co` and `OPS_SUPABASE_SERVICE_ROLE_KEY = <ops service_role key>` (the same value already stored as the cpeg-site Cloudflare secret from CC32). `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SERVICE_ROLE_KEY` are already set. No `config.toml` change (caller passes a valid user JWT, default verify_jwt is fine).
- **Adjacent exposure — CONFIRMED, needs its own scoped pass:** ops `product_licenses` is also anon-readable (probed 2026-07-05: anon SELECT returned all 5 rows incl. `license_key`, `customer_name`, `customer_email`). Same leak class as demo_leads. **Do NOT blind-lock it** — unlike demo_leads, products almost certainly verify keys by reading this table (Beacon/Navigator activation), so revoking anon SELECT could break live license activation. Correct fix mirrors Beacon's verify-attestation: a `verify-license` function products call with a key (returns valid/invalid), plus admin writes routed through a service-role function like ops-leads. Map every product's license-check read path BEFORE changing RLS. Spawned as a separate task.
- **Medium items not shipped (from the compliance review):** M-3 (in-band history rewrites are chained-but-invisible to the verify RPCs — a shared architecture limit, also Waypoint-side 077); L-2 ("no anchor = predate the audit log" wording could also cover a trigger-disabled row); L-3 (goals/monitors have no audit trigger — not hearing evidence). None are blockers.
- **DPA template** is markdown only — needs a rendered PDF/docx before a live superintendent close.

## 4. State / risks
- **Active risks: X-2 only** (infra/uptime monitoring) — unchanged. Navigator's N-1/N-7 remain resolved (CC32); N-2 (campus RLS) still Watch/demand-pull; this session did not trip it.
- The `demo_leads` exposure was a real trust-event-class issue (X-1 family) — now closed. Worth a memory: public anon keys on marketing pages → the target table must be anon-INSERT-only.

## 5. Verification summary
- Build: clean (`✓ built`), both before and after the migration-adjacent UI edits.
- Lint: 0 net-new errors (all flagged lines are pre-existing baseline patterns — Date.now purity, setState-in-effect — on untouched lines).
- Migration 086: applied by Kim, verified live via SQL editor (two booleans both true).
- demo_leads RLS: applied by Kim, verified live (SELECT blocked, INSERT works).
- Marketing HTML: verified rendering via local preview (home price, renewal, referral copy, no stale creds).

## What's next
1. Merge the PR (Cloudflare Pages + secret-scan gates; "Workers Builds" red is the known cosmetic stray connector — safe to merge over per CC31).
2. Delete the CC39 verify test row from ops `demo_leads`.
3. Optional: restore the /waypoint-admin demo-leads panel via a service_role read path.
4. Standing: X-2 uptime monitoring.
